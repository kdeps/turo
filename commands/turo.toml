description = "Point more, token less — reduce text to its content words"
prompt = "Pipe the current context through turo to strip stopwords and repeats, leaving only the content words that carry meaning. {{args}}"
