/turo
/dist/
# Old 23MB Moby source, no longer used — the map is now generated from WordNet
# by tools/gensyn.py. The baked map (thesaurus_data.go) is committed instead.
/thesaurus.txt

# Generated by `turo -install-agents` (npx skills add) — per-machine skill copies.
# skills-lock.json is committed to pin the skill source + hash.
/.agents/
/.qwen/
/.kdeps/
